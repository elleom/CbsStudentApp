class ChatMessage {
    constructor(createdDate, message, user) {
        this.createdDate = createdDate;
        this.message = message;
        this.user = user;
    }
}

export default ChatMessage;